// load mongoose models
var models = require('./models.js');

// abstract db queries for routers to use

// takes config obj (with all request schema fields) and saves a new request
function createRequest(config) {
	
}